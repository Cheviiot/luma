#!/bin/bash
set -euo pipefail
pkg_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
repo_root="$(cd "${pkg_dir}/.." && pwd)"
exec "${repo_root}/.github/scripts/package-update.sh" check "$(basename "$pkg_dir")"
