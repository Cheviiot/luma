#!/bin/bash
set -euo pipefail

package_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
repo_root="$(cd "${package_dir}/.." && pwd)"

exec "${repo_root}/.github/scripts/package-update.sh" check "$(basename "${package_dir}")"
